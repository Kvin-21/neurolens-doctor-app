name: neurolens_doctor
description: NeuroLens Doctor Portal - Desktop application for monitoring dementia patients
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
  intl: ^0.18.1
  window_manager: ^0.3.7
  uuid: ^4.2.1
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
